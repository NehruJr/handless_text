name: img_voice_to_text
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.4
  image_picker: ^0.8.4+10
  google_ml_kit: 0.7.3
  speech_to_text: ^5.4.3
  avatar_glow: ^2.0.2

flutter:

  uses-material-design: true

  # assets:

